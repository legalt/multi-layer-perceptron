{
  "name": "mlp",
  "version": "1.0.0",
  "description": "Multi layer perceptron with algorithm gradient regression in backpropogation",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild",
    "start": "node server.js"
  },
  "author": "LegAlt",
  "license": "ISC",
  "gypfile": true,
  "dependencies": {
    "node-static": "^0.7.10",
    "sharp": "^0.20.4"
  }
}
